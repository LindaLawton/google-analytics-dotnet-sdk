language: csharp
mono: none
dotnet: 2.1
dist: trusty

# Install the .NET Core 1.0 runtime as that's what we build and test against
addons:
  apt:
    sources:
    - sourceline: 'deb [arch=amd64] https://packages.microsoft.com/repos/microsoft-ubuntu-trusty-prod trusty main'
      key_url: 'https://packages.microsoft.com/keys/microsoft.asc'
    packages:
    - dotnet-sharedframework-microsoft.netcore.app-1.0.8
    - dotnet-sharedframework-microsoft.netcore.app-1.1.5

script:
  - dotnet build -c Release src/Google.Analytics.SDK.Core
  - dotnet build -c Release tests/Google.Analytics.SDK.Tests
  - dotnet test -c Release tests/Google.Analytics.SDK.Tests -no-build -c Release
  
  
  
